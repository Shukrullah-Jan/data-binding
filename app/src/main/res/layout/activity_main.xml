<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
            name="tvData"
            type="String" />

        <variable
            name="myUser"
            type="com.global.databinding.User" />
    </data>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        tools:context=".MainActivity">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="@{myUser.isActive ? `Active`: `Not Active`}"
            android:textSize="20sp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="@{myUser.name}"
            android:textSize="20sp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="@{String.valueOf(myUser.age)}"
            android:textSize="20sp" />

        <!--lambda notation is used for handling onClick listener for view binding-->
        <Button
            android:id="@+id/button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:enabled="@{myUser.active}"
            android:onClick="@{() -> myUser.invokeLogMessage()}"
            android:text="@{myUser.name}"
            android:textSize="20sp" />


        <ImageView
            android:id="@+id/imageView"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:loadImage="@{myUser.imageUrl}"
            android:src="@drawable/ic_launcher_background" />

    </LinearLayout>
</layout>